ktor:
  application:
    modules:
      - ru.tickets.ApplicationKt.module
  deployment:
    port: 8080

postgres:
  url: "jdbc:postgresql://localhost:5455/tickets"
  user: "tickets"
  password: "tickets"

api-keys:
  ramt: "ramt-secret"
  nations: "nations-secret"
  vakhtangov: "vakhtangov-secret"
  fomenki: "fomenki-secret"
  admin: "admin-secret"
